extends template.pug

block style
  link(rel="stylesheet", href="/static/stylesheets/register.css")

block content
  main.register
    figure
      img(src="/static/images/logo-ceo.png")
    p 필수 정보를 입력해주세요.
    form
      .id
        input(type="text" placeholder="아이디* (4자 이상)")
        span
      .pw
        input(type="password" placeholder="비밀번호* (8자 이상)")
        span
      .pw2
        input(type="password" placeholder="비밀번호 재확인*")
        span
      .row.mail
        input(type="text" placeholder="이메일 앞자리*")
        span @
        input(type="text" placeholder="이메일 뒷자리*") 
      select
        each val in ["naver.com", "hanmail.net", "nate.com", "hotmail.com", "gmail.com", "직접입력"]
          option= val
      .name
        input(type="text" placeholder="이름*")
        span
      .tel-auth
        .row
          input(type="tel" placeholder="휴대폰*")
          button.deactive 인증받기
        span
      ul
        li 인증번호 전송은 통신사에 따라 최대 1분까지 소요될 수 있습니다.
        li 인증번호가 도착하지 않을 경우 '인증번호 재전송'을 눌러주세요.
      
      .row.optional-check
        input(type="checkbox")
        span 선택 정보를 입력하시겠어요?
        a(href="#") 내용보기

      .row.address-find
        input(type="text" placeholder=" 우편번호")
        button.deactive 주소찾기
      input(type="text" placeholder="주소")
      input(type="text" placeholder="상세주소")
      
      .terms
        .term.term__all
          input(type="checkbox")
          span 전체 약관에 동의합니다.
        .term.term__mendatory
          input(type="checkbox")
          span 필수 항목에 동의합니다.
        span
          a(href="#") 배민사장님광장 이용약관
          a(href="#") 개인정보 수집이용 동의
        .term.term__optional
          input(type="checkbox")
          span 광고성 정보 수신 동의 (선택)
        ul
          li 만 14세 이상 회원만 가입 가능합니다. 
            a(href="#") 내용보기
      button.filled.wide 가입완료

  script(src="/static/js/register.js")
  script(src="/static/js/terms.js")